import 'package:flutter/material.dart';
class Auto extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return new Container(
      child: new Center(
        child: new Icon(Icons.timer, size: 150.0, color: Colors.brown)
      )
    ); 
  }
}
