import 'package:flutter/material.dart';

class Kunden extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return new Scaffold(
      appBar: new AppBar(title: new Text("Kunden"),backgroundColor: Colors.grey,
      ),
      );
  } 
}