import 'package:flutter/material.dart';

class Zeiten extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return new Scaffold(
      appBar: new AppBar(title: new Text("Erfasste Zeiten"),backgroundColor: Colors.grey,
      ),
      );
  } 
}